[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
addopts = --basetemp=tmp/pytest
markers =
    postgres: requires a PostgreSQL database and exercises real row locks
